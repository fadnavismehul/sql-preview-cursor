# Copy to .env.local and fill in your credentials (file is gitignored)

PRESTO_HOST=porta.data-engineering.myteksi.net
PRESTO_PORT=443
PRESTO_USER=mehul.fadnavis
PRESTO_PASSWORD=
PRESTO_CATALOG=hive
PRESTO_SCHEMA=default
PRESTO_SSL=true
PRESTO_SSL_VERIFY=true

// Optional: run a different SQL for probes
PRESTO_TEST_SQL=select 1
PRESTO_MAX_ROWS=500


